apiVersion: v1
kind: Service
metadata:
  name: workload-vitima
  namespace: {{ .Release.Namespace }}
  labels:
    app: workload-vitima
spec:
  selector:
    app: workload-vitima
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
